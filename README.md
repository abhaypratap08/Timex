# Timex

found the day to be boooorrriiiinnggggggg!!! so thought to make something just to revise JAVA     (:^{))
no thoughts given on the cosmetics... so, just chillout~~!!!!

## where is what ?
made two flavors of this thing. pick your poison:

* `ANALOG_TIMEX/` -> the classic one with the hands going round and round.
* `DIGITAL_TIMEX/` -> the digital numbers one (comes with a pomodoro timer if you actually want to be productive).

inside each of those:
* `src/` -> the raw `.java` files. go here to judge my Swing skills or compile it yourself.
* `dist/` -> the ready-to-run stuff. this is where the actual executable `.jar` files live.

## how to run >?

### LAZY ONES CAN JUSSS DOUBLE TAP ON THE .jar (inside the dist folders) I GUESS ??? IF IT AIN'T TOO MUCH TO DO.

(note: you gotta have java installed on your machine for the double-tap to work. if it doesn't open, your pc probably just needs the JRE. figure it out.)
